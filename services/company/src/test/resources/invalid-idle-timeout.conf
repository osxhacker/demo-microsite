# @(#) Company microservice configuration with invalid idle-timeout

include "application.conf"


# The idle-timeout parameter determines how long the HTTP server will
# wait for a request before closing a connection.  It must be between 10 seconds
# and 5 minutes.
idle-timeout = 1s

